const CODEX_SESSIONS_DIRNAME = 'sessions';
const CODEX_TAIL_BYTES_BASE = 8 * 1024 * 1024; // 8MB
const CODEX_HEAD_BYTES = 512 * 1024; // 512KB
const CODEX_SESSION_TAIL_BYTES_BASE = 256 * 1024; // 256KB
const CODEX_SESSION_TAIL_BYTES_MAX = 8 * 1024 * 1024; // 8MB max for single-session reads
const CODEX_DEDUP_BUCKET_MS = 60 * 1000; // 1 minute buckets for dedupe
const CODEX_SESSION_SUMMARY_TAIL_BYTES = 64 * 1024; // 64KB for session list previews
const CLAUDE_SESSION_SUMMARY_TAIL_BYTES = 64 * 1024; // 64KB for session list previews
const CODEX_SUMMARY_HEAD_BYTES = 32 * 1024; // 32KB for summary metadata
const CLAUDE_SUMMARY_HEAD_BYTES = 32 * 1024; // 32KB for summary metadata
const SUMMARY_READ_CONCURRENCY = 4;
const SESSION_INDEX_CACHE_TTL_MS = 30_000;
const SEARCH_FILELIST_CACHE_TTL_MS = 30_000;
const CLAUDE_LOG_TAIL_BYTES = 8 * 1024 * 1024; // 8MB
const CLAUDE_HEAD_BYTES = 256 * 1024; // 256KB
const CLAUDE_SESSION_TAIL_BYTES_BASE = 256 * 1024; // 256KB
const CLAUDE_SESSION_TAIL_BYTES_MAX = 8 * 1024 * 1024; // 8MB max for single-session reads

module.exports = {
  CLAUDE_HEAD_BYTES,
  CLAUDE_LOG_TAIL_BYTES,
  CLAUDE_SESSION_SUMMARY_TAIL_BYTES,
  CLAUDE_SESSION_TAIL_BYTES_BASE,
  CLAUDE_SESSION_TAIL_BYTES_MAX,
  CLAUDE_SUMMARY_HEAD_BYTES,
  CODEX_DEDUP_BUCKET_MS,
  CODEX_HEAD_BYTES,
  CODEX_SESSION_SUMMARY_TAIL_BYTES,
  CODEX_SESSION_TAIL_BYTES_BASE,
  CODEX_SESSION_TAIL_BYTES_MAX,
  CODEX_SESSIONS_DIRNAME,
  CODEX_SUMMARY_HEAD_BYTES,
  CODEX_TAIL_BYTES_BASE,
  SEARCH_FILELIST_CACHE_TTL_MS,
  SESSION_INDEX_CACHE_TTL_MS,
  SUMMARY_READ_CONCURRENCY,
};
